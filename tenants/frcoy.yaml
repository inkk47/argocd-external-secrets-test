argocd:
  valueFile: ""
  releaseName: ""
  appName: externalsecret-frcoy
  project: external-secrets-tls
  destinationNamespace: frcoy
  destinationServer: https://kubernetes.default.svc

externalSecret:
  metadata:
    name: certificate-secret
    namespace: frcoy
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: frcoy-tls
  data:
    - secretKey: cert
      remoteRef:
        key: latacc/dev/certificates/FrCoy
        property: crt
    - secretKey: key
      remoteRef:
        key: latacc/dev/certificates/FrCoy
        property: key
